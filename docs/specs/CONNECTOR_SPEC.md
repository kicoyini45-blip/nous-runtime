# CONNECTOR SPEC

Connectors declare lifecycle, actions, scopes, credential references, Workspace boundaries, risk, approval requirements, and audit behavior. Writes pass Governance and Approval before adapter execution.

## Compatibility

Versioned contracts must remain backward compatible during Alpha unless a security boundary requires fail-closed rejection.
