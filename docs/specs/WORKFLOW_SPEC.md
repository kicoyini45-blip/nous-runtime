# WORKFLOW SPEC

Workflow definitions remain in the Workflow Registry or Store and compile into existing TaskGraph, Run, Event, Governance, Approval, and Artifact contracts.

## Compatibility

Versioned contracts must remain backward compatible during Alpha unless a security boundary requires fail-closed rejection.
