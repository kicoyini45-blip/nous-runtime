# NODE SPEC

Nodes register identity, capabilities, locality, leases, and health through the Node Registry. Scheduling applies security, feasibility, fairness, and resource constraints before dispatch.

## Compatibility

Versioned contracts must remain backward compatible during Alpha unless a security boundary requires fail-closed rejection.
